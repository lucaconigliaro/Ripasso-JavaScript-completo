// Il tipo booleano può assumere solo due valori: true o false.
// Utilizzato per condizioni e controlli logici.

// Esempio:
let isAdmin = true;
if (isAdmin) {
    console.log('Accesso consentito');
} else {
    console.log('Accesso negato');
}