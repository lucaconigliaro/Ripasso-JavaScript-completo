// Permettono di eseguire codice solo se una condizione è soddisfatta.
// L’istruzione più comune è if, con else e else if per casi multipli.

// Esempio:
let eta = 20;
if (eta >= 18) {
    console.log('Sei maggiorenne');
} else if (eta > 13) {
    console.log('Sei adolescente');
} else {
    console.log('Sei minorenne');
}